CREATE TABLE IF NOT EXISTS {schema_name}.{table_name}
(
    num int64,
    numstr string,
    str string,
    comma string,
    doublequote string,
    quotecommaquote string,
    newlinestr string,
    date date,
    time time,
    timestamp datetime,
    timestamptz timestamp
);
